# 🔧 Исправление Google OAuth

## ✅ Что было исправлено

1. ✅ Создана страница `/auth/callback` для обработки OAuth возврата
2. ✅ Обновлена функция `signInWithGoogle` в `use-auth.ts`
3. ✅ Добавлен правильный `redirectTo` URL для deep linking
4. ✅ Добавлен маршрут в `_layout.tsx` для callback страницы

## 📋 Что нужно проверить в Supabase Dashboard

### 1. Настройка Redirect URLs

**ВАЖНО!** В Supabase Dashboard нужно добавить правильные Redirect URLs:

1. Зайди в [Supabase Dashboard](https://app.supabase.com/)
2. Выбери свой проект
3. Перейди в **Authentication** → **URL Configuration**
4. В разделе **Redirect URLs** добавь:
   ```
   nomiapp://auth/callback
   exp://localhost:8081
   ```

### 2. Проверка Google Provider

1. В Supabase Dashboard перейди в **Authentication** → **Providers**
2. Убедись, что **Google** включен
3. Проверь, что **Client ID** и **Client Secret** правильно указаны

### 3. Проверка в Google Cloud Console

В Google Cloud Console в **Authorized redirect URIs** должны быть:

```
https://[YOUR_PROJECT_REF].supabase.co/auth/v1/callback
nomiapp://auth/callback
```

(Замени `[YOUR_PROJECT_REF]` на свой project ref из Supabase Dashboard)

## 🧪 Как протестировать

1. **Запусти приложение:**
   ```bash
   npm start
   ```

2. **Нажми "Войти через Google"**

3. **Что должно произойти:**
   - Откроется браузер с Google авторизацией
   - После входа Google перенаправит на `nomiapp://auth/callback`
   - Приложение автоматически откроется
   - Пользователь будет перенаправлен на главный экран

4. **Проверь логи:**
   - В консоли должны быть логи `[Auth]` и `[AuthCallback]`
   - Если есть ошибки, они будут видны в консоли

## 🐛 Troubleshooting

### Проблема: "Redirect URI mismatch"

**Решение:**
- Убедись, что в Google Cloud Console добавлен правильный redirect URI
- Убедись, что в Supabase Dashboard добавлен `nomiapp://auth/callback`

### Проблема: Браузер открывается, но ничего не происходит

**Решение:**
- Проверь, что deep linking настроен в `app.json` (scheme: "nomiapp")
- Перезапусти приложение после изменений в `app.json`
- Проверь логи в консоли

### Проблема: Callback страница открывается, но пользователь не авторизован

**Решение:**
- Проверь, что Supabase правильно обрабатывает deep link
- Проверь логи `[AuthCallback]` в консоли
- Убедись, что сессия создается (проверь в Supabase Dashboard → Authentication → Users)

### Проблема: Ошибка "Provider not enabled"

**Решение:**
- В Supabase Dashboard → Authentication → Providers
- Включи Google provider
- Сохрани изменения

## 📱 Проверка Deep Linking

Для проверки deep linking вручную:

**iOS:**
```bash
xcrun simctl openurl booted "nomiapp://auth/callback"
```

**Android:**
```bash
adb shell am start -W -a android.intent.action.VIEW -d "nomiapp://auth/callback" com.joinway.nomiapp
```

## ✅ Готово!

После настройки всех Redirect URLs Google OAuth должен работать корректно!

Если проблемы остаются, проверь:
1. Логи в консоли приложения
2. Логи в Supabase Dashboard → Logs
3. Настройки в Google Cloud Console

