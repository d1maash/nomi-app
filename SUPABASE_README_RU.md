# 🗄️ База данных Supabase - Готово к использованию!

## ✅ Что уже настроено

### 1. База данных
Полностью настроенная база данных со следующими таблицами:

- ✅ **users** - Профили пользователей
- ✅ **transactions** - Транзакции (доходы и расходы)
- ✅ **budgets** - Бюджеты по категориям
- ✅ **goals** - Финансовые цели
- ✅ **ai_insights** - AI рекомендации и инсайты
- ✅ **challenges** - Игровые челленджи
- ✅ **badges** - Игровые достижения
- ✅ **anomaly_alerts** - Оповещения об аномалиях
- ✅ **user_settings** - Настройки пользователя
- ✅ **game_stats** - Игровая статистика

### 2. Безопасность (RLS)
✅ Row Level Security (RLS) политики настроены для всех таблиц
- Каждый пользователь видит только свои данные
- Полная изоляция данных между пользователями
- Защита от несанкционированного доступа

### 3. Интеграция с приложением
✅ Supabase клиент установлен и настроен
✅ TypeScript типы для всех таблиц
✅ Готовые хуки для работы с данными
✅ Real-time синхронизация
✅ Автоматическая синхронизация при входе пользователя

## 🚀 Быстрый старт

### Шаг 1: Создайте файл `.env`

Создайте файл `.env` в корне проекта `/Users/dimashi/Desktop/coding/nomi-app/nomi-app/`:

```env
EXPO_PUBLIC_SUPABASE_URL=https://dnkeulxxknyuqfjxjfrd.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRua2V1bHh4a255dXFmanhqZnJkIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjMxMzA2MzMsImV4cCI6MjA3ODcwNjYzM30.JSyXlv60GQ1umVCBM4_XjrKapWOImLOoZB-OFlsxL9M
```

### Шаг 2: Перезапустите приложение

```bash
npm start
```

### Шаг 3: Войдите в приложение

При входе через Clerk данные автоматически синхронизируются с Supabase!

## 📖 Примеры использования

### Работа с транзакциями

```typescript
import { useTransactions } from '@/hooks/use-supabase';

function MyScreen() {
  const { transactions, isLoading, add, update, remove } = useTransactions();

  // Добавить транзакцию
  async function addTransaction() {
    await add({
      amount: 1500,
      category: 'food',
      description: 'Обед',
      date: new Date(),
      type: 'expense',
    });
  }

  // Обновить транзакцию
  async function updateTransaction(id: string) {
    await update(id, { amount: 2000 });
  }

  // Удалить транзакцию
  async function deleteTransaction(id: string) {
    await remove(id);
  }

  if (isLoading) return <ActivityIndicator />;

  return (
    <FlatList
      data={transactions}
      renderItem={({ item }) => (
        <TransactionItem transaction={item} />
      )}
    />
  );
}
```

### Работа с бюджетами

```typescript
import { useBudgets } from '@/hooks/use-supabase';

function BudgetsScreen() {
  const { budgets, add, update, remove } = useBudgets();

  async function createBudget() {
    await add({
      category: 'food',
      limit: 50000,
      period: 'monthly',
      startDate: new Date(),
      endDate: new Date(Date.now() + 30 * 24 * 60 * 60 * 1000),
    });
  }

  return (
    <View>
      {budgets.map(budget => (
        <BudgetCard key={budget.id} budget={budget} />
      ))}
    </View>
  );
}
```

### Работа с целями

```typescript
import { useGoals } from '@/hooks/use-supabase';

function GoalsScreen() {
  const { goals, add, update } = useGoals();

  async function createGoal() {
    await add({
      name: 'Отпуск',
      targetAmount: 500000,
      currentAmount: 0,
      deadline: new Date('2025-07-01'),
      category: 'travel',
    });
  }

  return (
    <View>
      {goals.map(goal => (
        <GoalCard key={goal.id} goal={goal} />
      ))}
    </View>
  );
}
```

### Ручная синхронизация

```typescript
import { useSupabase } from '@/components/supabase-provider';

function SettingsScreen() {
  const { syncData, isSyncing, lastSyncTime } = useSupabase();

  return (
    <View>
      <Text>
        Последняя синхронизация: 
        {lastSyncTime?.toLocaleString('ru-RU') || 'Никогда'}
      </Text>
      <Button 
        title="Синхронизировать" 
        onPress={syncData}
        disabled={isSyncing}
      />
    </View>
  );
}
```

## 🎯 Основные возможности

### Real-time синхронизация
Все изменения в базе данных автоматически отображаются в приложении в реальном времени. Даже если изменения произошли на другом устройстве!

### Автоматическая синхронизация
При входе пользователя через Clerk все данные автоматически загружаются из Supabase.

### Offline-first подход
Данные кэшируются локально и синхронизируются с сервером при наличии соединения.

### Типизация TypeScript
Все типы базы данных полностью типизированы для безопасной работы с данными.

### Безопасность
Row Level Security гарантирует, что каждый пользователь видит только свои данные.

## 📚 Документация

- **SUPABASE_SETUP.md** - Подробная документация по настройке
- **docs/SUPABASE_USAGE_EXAMPLES.md** - Примеры использования с кодом
- **types/database.ts** - TypeScript типы для всех таблиц

## 🔧 Доступные хуки

### Основные хуки:
- `useSupabaseData()` - Базовая информация о пользователе
- `useTransactions()` - Работа с транзакциями
- `useBudgets()` - Работа с бюджетами
- `useGoals()` - Работа с целями
- `useInsights()` - Работа с AI инсайтами
- `useChallenges()` - Работа с челленджами
- `useSupabaseSync()` - Универсальная синхронизация

### Провайдер:
- `<SupabaseProvider>` - Автоматическая инициализация и синхронизация
- `useSupabase()` - Доступ к состоянию синхронизации

## 🛠️ Функции для прямого доступа

Все функции находятся в `services/supabase-sync.ts`:

### Транзакции:
- `getTransactions(userId)` - Получить все транзакции
- `createTransaction(userId, transaction)` - Создать транзакцию
- `updateTransaction(id, updates)` - Обновить транзакцию
- `deleteTransaction(id)` - Удалить транзакцию

### Бюджеты:
- `getBudgets(userId)` - Получить все бюджеты
- `createBudget(userId, budget)` - Создать бюджет
- `updateBudget(id, updates)` - Обновить бюджет
- `deleteBudget(id)` - Удалить бюджет

### Цели:
- `getGoals(userId)` - Получить все цели
- `createGoal(userId, goal)` - Создать цель
- `updateGoal(id, updates)` - Обновить цель
- `deleteGoal(id)` - Удалить цель

### И многие другие...

## 💡 Best Practices

1. **Всегда используйте хуки** в компонентах React для автоматической синхронизации
2. **Используйте функции напрямую** только вне компонентов или для сложной логики
3. **Обрабатывайте ошибки** с помощью try/catch
4. **Используйте TypeScript типы** для безопасности
5. **Тестируйте offline режим** для проверки кэширования

## 🐛 Troubleshooting

### Проблема: "Missing Supabase environment variables"
**Решение:** Создайте файл `.env` с переменными окружения (см. Шаг 1)

### Проблема: Данные не синхронизируются
**Решение:** 
1. Проверьте, что пользователь авторизован через Clerk
2. Проверьте консоль на ошибки
3. Убедитесь, что SupabaseProvider обернут вокруг приложения

### Проблема: Real-time не работает
**Решение:** 
1. Проверьте подключение к интернету
2. Убедитесь, что Supabase Realtime включен в проекте
3. Проверьте консоль на ошибки подписок

## 📊 Статистика проекта

- ✅ **10 таблиц** в базе данных
- ✅ **50+ RLS политик** для безопасности
- ✅ **8 готовых хуков** для работы с данными
- ✅ **30+ функций** для прямого доступа к БД
- ✅ **Real-time синхронизация** для всех таблиц
- ✅ **TypeScript типизация** для всех сущностей

## 🎉 Готово к использованию!

Ваша база данных Supabase полностью настроена и готова к использованию. Просто создайте файл `.env` и начинайте разрабатывать!

Если возникнут вопросы, обратитесь к документации:
- SUPABASE_SETUP.md - для подробностей о настройке
- docs/SUPABASE_USAGE_EXAMPLES.md - для примеров кода

